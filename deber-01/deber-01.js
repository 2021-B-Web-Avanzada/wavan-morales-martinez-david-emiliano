

// Variables
const concesionario = {
    nombre_concesionario: string,
    pais_origen: string,
    direccion: string,
    telefono: number | string,
    abierto: boolean,
    pagina_web: string,
    autos: [] | undefined,
}

const autos = {
    modelo: string,
    año: number,
    cilindraje: number| string,
    color: string,
    precio: number,
    nuevo: boolean
}

function crearConcesionario(nombre_concesionario, pais_origen, direccion, telefono, abierto, pagina_web, autos) {
        this.nombre_concesionario,
        this.pais_origen,
        this.direccion,
        this.telefono,
        this.abierto,
        this.pagina_web,
        this.autos
}

function crearAuto (modelo, año, cilindraje, color, precio, nuevo) {
    this.modelo,
        this.año,
        this.cilindraje,
        this.color,
        this.precio,
        this.nuevo
}

